
const SectionTitle = ({title}) => {
    return (
        <section>
            <div className='custom-container'>
            <div className="custom-row">
                <h3 className='custom-border mx-auto'>{title}</h3> 
            </div>
            </div>
        </section>
    );
};

export default SectionTitle;